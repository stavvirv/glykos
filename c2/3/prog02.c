#include <stdio.h>
#include <math.h>
#include <string.h>
int main()

{

int length;
char seq[5000];
while(scanf("%s", seq ) == 1 )
{
	length = strlen ( seq );
	printf("%s %d\n", seq, length);
}






}
